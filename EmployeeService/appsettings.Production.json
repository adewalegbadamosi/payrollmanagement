{
    "ConnectionStrings": {
        "DbConnectionString": "Data Source=172.17.0.2,1433;Initial Catalog=Finance;User Id=sa;Password=Password10$;"
      },

    "AuthService": "http://localhost:80/api/v1/auth",   

    "RabbitMQHost": "172.17.0.3", //If you, in a container, want to connect to a service in another container, you need to use the ip address of that container, or a hostname that resolves to the ip address of that container (using host will only work if the containers are running on same user defined network).
    "RabbitMQPort": 5672,
    "RabbitMQUser": "guest",
    "RabbitMQPass": "guest" 
}
